{
  "name": "toy-blog-engine",
  "version": "0.1",
  "description": "Toy blog engine that lets you specify a template and publish HTML/JS posts using it with some simple navigation features.",
  "license": "MIT",
  "bin": {
    "toyb": "cli.js"
  },
  "scripts": {
    "build": "esbuild --bundle --outfile=cli.js --platform=node cli.ts"
  },
  "devDependencies": {
    "@types/jsdom": "^21.1.6",
    "@types/node": "^18.19.2",
    "@types/parse5": "^7.0.0",
    "@types/yargs": "^17.0.32",
    "esbuild": "^0.19.9",
    "typescript": "^3.7.4"
  },
  "dependencies": {
    "jsdom": "^23.0.1",
    "yargs": "^17.7.2"
  }
}
